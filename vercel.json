{
  "version": 2,
  "buildCommand": "npm run build && npm run server:build",
  "framework": null,
  "builds": [{ "src": "dist/server/index.js", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "dist/server/index.js" }]
}
